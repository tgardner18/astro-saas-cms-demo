---
import { DisplaySettingsFragment, HeadingElementFragment } from '../../../../../services/graphql/__generated/sdk'
import { getHeadingElementStyles } from './HeadingStyling'
import { ContentPayload } from '../../../../../services/shared/ContentPayload'
export interface Props {
  data: HeadingElementFragment;
  contentPayload: ContentPayload;
  displaySettings: DisplaySettingsFragment[];
}
const { data, displaySettings } = Astro.props as Props
---

<div class:list={getHeadingElementStyles(displaySettings)}>{data.headingText}</div>

