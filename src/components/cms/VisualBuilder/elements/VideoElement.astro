---
import {IDisplaySettingsFragment, IVideoElementFragment} from "../../../../services/graphql/__generated/sdk";

export interface Props {
    element: IVideoElementFragment;
    displaySettings: IDisplaySettingsFragment[];
}
const {element, displaySettings} = Astro.props as Props;
---

<video src={element.video.url.default} autoplay muted/>
