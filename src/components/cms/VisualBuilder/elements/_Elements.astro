---
import HeadingElement from "./HeadingElement.astro";
import CTAElement from "./CTAElement.astro";
import ImageElement from "./ImageElement.astro";
import VideoElement from "./VideoElement.astro";
import ParagraphElement from "./ParagraphElement.astro";
const {element} = Astro.props.element;
const elementType = element._metadata.types[0];
---

{elementType === "HeadingElement" &&
<HeadingElement element={element}/>}
{elementType === "CTAElement" &&
<CTAElement element={element}/>}
{elementType === "ImageElement" &&
<ImageElement element={element}/>}
{elementType === "VideoElement" &&
<VideoElement element={element}/>}
{elementType === "ParagraphElement" &&
<ParagraphElement element={element}/>}
