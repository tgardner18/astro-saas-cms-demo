---
import {gql} from "graphql-request";
import {IImageElementFragment} from "../../../../services/graphql/__generated/sdk";

export interface Props {
    element: IImageElementFragment;
}
const {element} = Astro.props as Props;
---

<img src={element.imageLink.url.default} alt={element.altText}/>
