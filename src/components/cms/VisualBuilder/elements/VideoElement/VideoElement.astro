---
import { DisplaySettingsFragment, VideoElementFragment } from '../../../../../services/graphql/__generated/sdk'
import { ContentPayload } from '../../../../../services/shared/ContentPayload'

export interface Props {
  element: VideoElementFragment;
  displaySettings: DisplaySettingsFragment[];
  contentPayload: ContentPayload;
}
const { element } = Astro.props as Props
---

<video src={element.video.url.default} autoplay muted />
