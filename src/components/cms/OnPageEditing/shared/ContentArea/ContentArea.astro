---
import BlogListingBlock from '../../components/BlogListingBlock/BlogListingBlock.astro'
import { ContentPayload } from '../../../../../services/shared/ContentPayload'
export interface Props {
  contentAreaItem: any;
  contentPayload: ContentPayload;
}
const { contentAreaItem, contentPayload } = Astro.props as Props
const componentType = contentAreaItem?._metadata?.types?.[0]
console.log('IN ContentArea', contentPayload)
---
{componentType === 'BlogListingBlock' &&
<BlogListingBlock contentPayload={contentPayload} data={contentAreaItem} /> }
